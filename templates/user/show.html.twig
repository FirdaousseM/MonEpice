{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>LastName</th>
                <td>{{ user.lastName }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ user.firstName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ user.phone }}</td>
            </tr>
            <tr>
                <th>Adresses</th>
                {% for address in user.addresses %}
                  <td>{{ address.content }}</td>
                {% endfor %}
            </tr>
        </tbody>
    </table>

<div>
  <!--
    <button class="dropdown-menu-activator" id="addAddressFormBtn">Ajouter une adresse</button>
    <div class="dropdown-menu-content" id="addAddressForm">
      {{ include('address/_form.html.twig') }}
    </div>
    -->
</div>    

    <a href="{{ path('app_user_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
